server:
  port: 8080

debug: true

logging:
  level:
    root: DEBUG
    org:
      springframework:
        security: trace

address:  http://auth-server:8080  #https://fiscaliza-auth-server.herokuapp.com/